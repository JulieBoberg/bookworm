import React from "react";
import PropTypes from "prop-types";

const Dashboard = props => {
  return <div style={{ font: "black" }}>Dashboard</div>;
};

Dashboard.propTypes = {};

export default Dashboard;
